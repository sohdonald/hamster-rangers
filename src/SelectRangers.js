import React, {useState} from "react";

/* this component displays a menu with all the rangers we selected from
  AssembleRangers component.  After 5 rangers are selected, it displays the
  "summon megazord" button. 
  clicking the "summon megazord" will display a megazord from the db.json that fulfills
  the conditions in the summonBy key
  
  We are also going to need to fetch the megazords

  first, 
 */

  function SelectRangers() {
    // display the ranger cards we clicked on
    return(
    <div>
{/* we need to display the rangers we selected */}
    </div>
    );
  }

  export default SelectRangers;




